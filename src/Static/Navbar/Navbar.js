import React from 'react';
import s from './Navbar.module.css';

const Header = () =>{
    return(
        <nav className={}>
            <div>

            </div>
            <menu>

            </menu>
            <nav>

            </nav>
        </nav>
    );
}

export default Header;